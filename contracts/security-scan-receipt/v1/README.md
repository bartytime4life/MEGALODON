# Security scan receipt v1

This contract normalizes scanner output without making any scanner authoritative.
It is suitable for exact-head evidence retention once a scanner action exposes a
deterministic export path.

The repository does not include a Codex Security invocation in this change.
Scanner execution, artifact retention, ruleset admission, triage, suppression
approval, independent review, and merge remain separate decisions.
